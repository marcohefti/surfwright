# Mission 004 - first-pass-orientation

Task
- Use SurfWright to complete this browser mission.
- Start URL: https://developer.mozilla.org/en-US/
- Goal: count visible top-level main navigation items without hover

Output format
Return exactly one JSON object with these keys:
- navItemsCount

Constraints
- Use the exact key names listed above.
- Return JSON only (no markdown fences, no prose).
