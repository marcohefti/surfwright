# Mission 007 - style-inspection

Task
- Use SurfWright to complete this browser mission.
- Start URL: https://getbootstrap.com/docs/5.3/components/buttons/
- Goal: read computed padding-top on the primary button

Output format
Return exactly one JSON object with these keys:
- paddingTopPx

Constraints
- Use the exact key names listed above.
- Return JSON only (no markdown fences, no prose).
