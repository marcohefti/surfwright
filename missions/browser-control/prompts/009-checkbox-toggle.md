# Mission 009 - checkbox-toggle

Task
- Use SurfWright to complete this browser mission.
- Start URL: https://the-internet.herokuapp.com/shifting_content
- Goal: open Example 2 and return absolute src URL of the rendered image

Output format
Return exactly one JSON object with these keys:
- imageSrc

Constraints
- Use the exact key names listed above.
- Return JSON only (no markdown fences, no prose).
